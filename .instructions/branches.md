# Branches

- Keep the published umbrella baseline on `main`.
- Use short-lived `update/<topic>` branches for reviewed umbrella changes.
- A member owns its own branch and release policy.
- A `.gitmodules` branch hint does not replace the pinned gitlink revision.
